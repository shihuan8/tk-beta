# TK Beta

TK Beta panel custom build.

## Components

- `go-backend`: panel API service
- `vite-frontend`: web dashboard
- `go-gost`: bundled node agent

## Release

Tagged releases build Docker images and node agent binaries through GitHub Actions.
